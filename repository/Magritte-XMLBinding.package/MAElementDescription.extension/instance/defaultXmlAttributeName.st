*magritte-xmlbinding
defaultXmlAttributeName
	"Answer a default xml attribute name. Use the readSelector if present.
	Otherwise convert the label to a valid attribute name."
	
	^self defaultXmlElementName